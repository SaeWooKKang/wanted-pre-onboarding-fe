import React from 'react';
import styled from 'styled-components';

const Logo = () => {
  return <Wrapper>Instagram</Wrapper>;
};

const Wrapper = styled.h1`
  margin: 12px 0 24px 0;
  color: black;
  font-size: 35px;
`;

export default Logo;
